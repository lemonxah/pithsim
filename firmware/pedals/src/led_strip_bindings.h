// Extra esp-idf-sys bindings for the espressif/led_strip component (RMT WS2812/
// SK6812 driver). Clean C API — merged into esp_idf_svc::sys::*.
#if defined(ESP_IDF_COMP_ESPRESSIF__LED_STRIP_ENABLED)
#include "led_strip.h"
#endif

dependencies:
  # Raw TinyUSB (NOT espressif/esp_tinyusb — its wrapper can't be bindgen'd here).
  # 0.19.x is the version the component manager resolved alongside ESP-IDF 5.3.
  espressif/tinyusb: "~0.19.0"
